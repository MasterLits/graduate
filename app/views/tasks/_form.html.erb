<%= form_for(task) do |f| %>
  <% if task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task.errors.count, "error") %> prohibited this task from being saved:</h2>
      <ul>
      <% task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


    <div class="field">
      <%= f.label :category_id_id, "Выберите категорию" %>
        <%=f.select :category_id, ['Выбeрите категорию']+Category.all.collect{|s| [s.body, s.id]}%>
      </div>
    <div class="field">
      <%= f.label :status_id, "Выберите cтатус выполнения задания" %>
      <%=f.select :status_id, ['Выбирите статус']+Status.all.collect{|s| [s.body, s.id]}%>
    </div>
    <div class="field">
      <%= f.label :title, "Заголовок" %>
      <%= f.text_field :title %>
    </div>

  <div class="field">
    <%= f.label :body, "Описание"%>
    <%= f.text_area :body %>
  </div>

  <div class="field">
    <%= f.label :cost, "Цена" %>
    <%= f.number_field :cost %>
  </div>


  <div class="field">
    <%= f.label :location, "Место" %>
    <%= f.text_field :location %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
